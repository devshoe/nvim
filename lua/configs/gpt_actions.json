{
  "document": {
    "type": "chat",
    "opts": {
      "template": "generate documentation for this `{{filetype}}` function, list all arguments, and example sample input and expected output. your answer should not exceed 250 words. write it as though you are a plugin on an ide: {{input}}",
      "strategy": "display",
      "params": {
        "model": "gpt-3.5-turbo",
        "stop": [
          "```" 
        ]
      }
    }
  }
}
